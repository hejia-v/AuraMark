# AuraMark 技术开发文档

版本：v1.1  
日期：2026-03-10

## 1. 技术目标

本文档定义 AuraMark 的核心技术架构、项目分层、主要数据结构、关键算法、WPF Surface 实现方向、测试策略与开工顺序。

目标：

- 形成一套 **可测试、可迭代、可落地** 的 WPF 原生 Markdown 编辑器技术方案
- 将会话中的所有关键结论、代码骨架与实现顺序收敛成文档与模板源码

---

## 2. 技术总路线

- 文本模型：`SourceText` / `TextSpan`
- 语法层：Markdown block / inline parser
- 编辑层：Action / Reducer / Undo-Redo
- 布局层：WPF `TextFormatter` + `TextSource`
- 渲染层：`FrameworkElement.OnRender` + `DrawingContext`
- 历史：`ImmutableStack<T>`

---

## 3. 解决方案结构

```text
AuraMark.sln
 ├─ AuraMark.Core.Text
 ├─ AuraMark.Core.Syntax
 ├─ AuraMark.Core.Editing
 ├─ AuraMark.Core.Layout
 ├─ AuraMark.Wpf
 └─ AuraMark.Tests
```

依赖方向：

```text
Core.Text
  ↓
Core.Syntax
  ↓
Core.Editing
  ↓
Core.Layout
  ↓
Wpf
```

---

## 4. 项目职责

### AuraMark.Core.Text

负责：

- 文本快照
- 文本版本
- 选区
- 文本编辑应用（TextEdit -> SourceText）

### AuraMark.Core.Syntax

负责：

- Markdown block/inline 结构
- ParseResult
- 增量重算窗口扩张
- 全量/增量 parse

### AuraMark.Core.Editing

负责：

- action 定义
- reducer
- state
- effect
- invalidation
- undo/redo

### AuraMark.Core.Layout

负责：

- TextRunProperties
- TextSource
- paragraph/code fence layout
- LayoutDocument
- caret/selection 几何映射

### AuraMark.Wpf

负责：

- EditorSurfaceControl
- 输入采集
- hit test
- rendering
- blink
- effect 执行

### AuraMark.Tests

负责：

- 数据层测试
- parser 测试
- reducer 测试
- layout 测试

---

## 5. Core.Text

### 5.1 核心类型

```csharp
public readonly record struct TextPosition(int Offset)
{
    public static TextPosition Zero => new(0);
}
```

```csharp
public readonly record struct SelectionRange(TextPosition Anchor, TextPosition Active)
{
    public bool IsCollapsed => Anchor.Offset == Active.Offset;
    public int Start => Math.Min(Anchor.Offset, Active.Offset);
    public int End => Math.Max(Anchor.Offset, Active.Offset);

    public TextSpan AsTextSpan() => TextSpan.FromBounds(Start, End);

    public static SelectionRange Collapsed(TextPosition position) => new(position, position);
}
```

```csharp
public sealed record DocumentSnapshot(
    SourceText Text,
    DocumentVersion Version,
    SelectionRange Selection,
    EditorOptions Options,
    DocumentMeta Meta);
```

### 5.2 文本编辑接口

```csharp
public readonly record struct TextEdit(TextSpan Span, string NewText);

public sealed record TextApplyResult(
    DocumentSnapshot Snapshot,
    IReadOnlyList<TextChange> Changes,
    TextSpan DirtySpan);

public interface ITextBufferService
{
    TextApplyResult Apply(DocumentSnapshot snapshot, IReadOnlyList<TextEdit> edits);
}
```

### 5.3 SourceTextBufferService

```csharp
public sealed class SourceTextBufferService : ITextBufferService
{
    public TextApplyResult Apply(DocumentSnapshot snapshot, IReadOnlyList<TextEdit> edits)
    {
        if (edits.Count == 0)
            return new TextApplyResult(snapshot, Array.Empty<TextChange>(), default);

        var ordered = edits.OrderByDescending(e => e.Span.Start).ToArray();
        var text = snapshot.Text;
        TextSpan? dirty = null;

        foreach (var edit in ordered)
        {
            text = text.Replace(edit.Span, edit.NewText);
            dirty = dirty is null
                ? TextSpan.FromBounds(edit.Span.Start, edit.Span.Start + edit.NewText.Length)
                : TextSpan.FromBounds(
                    Math.Min(dirty.Value.Start, edit.Span.Start),
                    Math.Max(dirty.Value.End, edit.Span.Start + edit.NewText.Length));
        }

        var changes = text.GetTextChanges(snapshot.Text).ToArray();
        var next = snapshot with
        {
            Text = text,
            Version = snapshot.Version.Next(),
            Meta = snapshot.Meta with { IsDirty = true }
        };

        return new TextApplyResult(next, changes, dirty ?? default);
    }
}
```

---

## 6. Core.Syntax

### 6.1 抽象

```csharp
public abstract record MdNode(TextSpan Span);
public abstract record MdBlock(TextSpan Span, MdBlockKind Kind) : MdNode(Span);
public abstract record MdInline(TextSpan Span, MdInlineKind Kind) : MdNode(Span);
```

### 6.2 Block 类型

- ParagraphBlock
- HeadingBlock
- QuoteBlock
- ListBlock
- ListItemBlock
- CodeFenceBlock
- TableBlock
- ThematicBreakBlock

### 6.3 Inline 类型

- TextInline
- EmphasisInline
- StrongInline
- CodeInline
- LinkInline

### 6.4 ParseResult

```csharp
public sealed record ParseResult(
    DocumentSnapshot Snapshot,
    IReadOnlyList<MdBlock> Blocks,
    IReadOnlyList<OutlineItem> Outline,
    IReadOnlyList<ParseDiagnostic> Diagnostics,
    IReadOnlyList<BlockMapEntry> BlockMap);
```

### 6.5 增量解析策略

原则：

- 第一版先做块级安全增量
- dirty span 向上下文扩张到安全边界
- fence、list、quote、table 区域做保守扩张
- inline 先整块重算

### 6.6 主流程

```csharp
public ParseResult Parse(DocumentSnapshot snapshot, ParseResult? previous, TextSpan dirtySpan)
{
    if (previous is null)
        return FullParse(snapshot);

    var window = ExpandToSafeWindow(snapshot.Text, previous, dirtySpan);
    var blocks = ParseBlocks(snapshot.Text, 0, snapshot.Text.Length); // 第一版可先全量

    return new ParseResult(
        snapshot,
        blocks,
        BuildOutline(snapshot.Text, blocks),
        Array.Empty<ParseDiagnostic>(),
        BuildBlockMap(blocks));
}
```

### 6.7 ReparseWindowExpander

第一版规则：

- 空行：硬边界
- Atx heading：硬边界
- fence：整 fence 扩张
- list / quote / continuation：继续扩
- table 区域：继续扩
- 普通 paragraph continuation：继续扩

实现见源码模板 `ReparseWindowExpander.cs`。

---

## 7. Core.Editing

### 7.1 Action 列表

```csharp
public interface IEditorAction { }

public sealed record InsertTextAction(TextPosition Position, string Text) : IEditorAction;
public sealed record ReplaceRangeAction(TextSpan Span, string Text) : IEditorAction;
public sealed record ReplaceSelectionWithTextAction(string Text) : IEditorAction;
public sealed record DeleteBackwardAction() : IEditorAction;
public sealed record DeleteForwardAction() : IEditorAction;
public sealed record InsertLineBreakAction() : IEditorAction;
public sealed record SetSelectionAction(SelectionRange Selection) : IEditorAction;
public sealed record MoveCaretAction(CaretMoveKind Kind, bool ExtendSelection) : IEditorAction;
public sealed record UndoAction() : IEditorAction;
public sealed record RedoAction() : IEditorAction;
```

### 7.2 State

```csharp
public sealed record EditorState(
    DocumentSnapshot Snapshot,
    ParseResult? Parse,
    LayoutDocument? Layout,
    ViewportState Viewport,
    UndoRedoState History,
    UiTransientState UiState,
    EditorVisualState VisualState);
```

### 7.3 Invalidations / Effects

```csharp
public abstract record EditorEffect;
public sealed record ScrollIntoViewEffect(TextSpan Span) : EditorEffect;
public sealed record RequestRenderEffect() : EditorEffect;

public abstract record EditorInvalidation;
public sealed record SyntaxInvalidation(TextSpan Span) : EditorInvalidation;
public sealed record LayoutInvalidation(TextSpan Span) : EditorInvalidation;
public sealed record VisualInvalidation(TextSpan Span) : EditorInvalidation;
```

### 7.4 文本变更统一 rebuild

```csharp
private ReduceResult RebuildAfterTextChange(
    EditorState oldState,
    DocumentSnapshot changedSnapshot,
    IReadOnlyList<TextChange> changes,
    TextSpan dirtySpan,
    SelectionRange newSelection,
    string transactionName)
{
    var finalSnapshot = changedSnapshot with { Selection = newSelection };
    var parse = _parser.Parse(finalSnapshot, oldState.Parse, dirtySpan);
    var layout = _layout.Build(new LayoutBuildRequest(parse, oldState.Viewport, dirtySpan), oldState.Layout);

    var rebuilt = oldState with
    {
        Snapshot = finalSnapshot,
        Parse = parse,
        Layout = layout.Document,
        VisualState = oldState.VisualState with
        {
            Caret = oldState.VisualState.Caret with { Position = newSelection.Active },
            Selection = new SelectionVisualState(newSelection)
        }
    };

    var withHistory = PushTransaction(oldState, rebuilt, transactionName);

    return new ReduceResult(
        withHistory,
        new EditorInvalidation[]
        {
            new SyntaxInvalidation(dirtySpan),
            new LayoutInvalidation(dirtySpan),
            new VisualInvalidation(dirtySpan)
        },
        new EditorEffect[]
        {
            new RequestRenderEffect(),
            new ScrollIntoViewEffect(newSelection.AsTextSpan())
        });
}
```

### 7.5 ReplaceCurrentSelection helper

```csharp
private ReduceResult ReplaceCurrentSelection(EditorState state, string text, string txName)
{
    var selection = state.Snapshot.Selection;

    TextEdit edit;
    int newCaretOffset;

    if (!selection.IsCollapsed)
    {
        edit = new TextEdit(selection.AsTextSpan(), text);
        newCaretOffset = selection.Start + text.Length;
    }
    else
    {
        edit = new TextEdit(new TextSpan(selection.Active.Offset, 0), text);
        newCaretOffset = selection.Active.Offset + text.Length;
    }

    var apply = _textBuffer.Apply(state.Snapshot, new[] { edit });
    return RebuildAfterTextChange(state, apply.Snapshot, apply.Changes, apply.DirtySpan, SelectionRange.Collapsed(new TextPosition(newCaretOffset)), txName);
}
```

### 7.6 关键 reducer 动作

- ReplaceSelectionWithTextAction
- DeleteBackwardAction
- DeleteForwardAction
- InsertLineBreakAction
- MoveCaretAction
- UndoAction
- RedoAction

完整代码见模板中的 `EditorReducer.cs`。

---

## 8. Undo / Redo

### 8.1 历史事务

```csharp
public sealed record EditorTransaction(string Name, DocumentSnapshot Before, DocumentSnapshot After);
```

### 8.2 栈结构

```csharp
public sealed record UndoRedoState(
    ImmutableStack<EditorTransaction> UndoStack,
    ImmutableStack<EditorTransaction> RedoStack)
{
    public static UndoRedoState Empty =>
        new(ImmutableStack<EditorTransaction>.Empty, ImmutableStack<EditorTransaction>.Empty);
}
```

### 8.3 服务实现

`UndoRedoService` 使用 `ImmutableStack<T>` 实现 push / pop undo / pop redo，模板中已包含完整代码。

### 8.4 第一版策略

- 直接保存 Before / After Snapshot
- undo/redo 时直接恢复 snapshot
- dirty span 先全量

这不是最终优化版，但非常稳。

---

## 9. Core.Layout

### 9.1 LayoutDocument

```csharp
public sealed record LayoutDocument(
    long Version,
    IReadOnlyList<LayoutBlock> Blocks,
    double TotalHeight)
{
    public void DisposeLines() { ... }
}
```

### 9.2 Block Layout 类型

- LayoutParagraphBlock
- LayoutHeadingBlock
- LayoutCodeFenceBlock

### 9.3 TextLineLayout / CodeLineLayout

```csharp
public sealed record TextLineLayout(
    TextSpan Span,
    Rect Bounds,
    int LineIndex,
    TextLine TextLine,
    int StartOffset,
    int Length);
```

### 9.4 ThemeMetrics

```csharp
public sealed record ThemeMetrics(
    double PageWidth,
    double ParagraphSpacing,
    double BlockSpacing,
    double CodeBlockPadding,
    double CodeToolbarHeight,
    Typeface BodyTypeface,
    double BodyFontSize,
    Typeface CodeTypeface,
    double CodeFontSize);
```

---

## 10. TextRunProperties / TextSource

### 10.1 SimpleTextRunProperties

模板中包含 `SimpleTextRunProperties` 实现，用于提供：

- Typeface
- Font size
- Foreground / Background
- Text decorations
- Culture
- Typography

### 10.2 Property Factory

- Body
- Emphasis
- Strong
- InlineCode
- Link
- CodeLine

### 10.3 ParagraphTextSource

- 输入：ParagraphBlock + SourceText + PropertyFactory
- 内部通过 `InlineRunBuilder` 构建 runs
- `GetTextRun` 返回覆盖 offset 的 run
- 超出段尾返回 `TextEndOfParagraph`

### 10.4 CodeLineTextSource

- 每个逻辑代码行一个 source
- 简化 code fence 布局与行号同步

---

## 11. LayoutEngine

### 11.1 ParagraphLayouter

```csharp
public LayoutParagraphBlock LayoutParagraph(ParagraphBlock block, SourceText text, double x, double y, double width)
{
    var source = ParagraphTextSource.Create(block, text, _propertyFactory);
    var paragraphProps = new BasicParagraphProperties(_propertyFactory.CreateBody());

    var lines = new List<TextLineLayout>();
    double currentY = y;
    int absolutePosition = block.Span.Start;
    int lineIndex = 0;

    while (absolutePosition < block.Span.End)
    {
        var textLine = _formatter.FormatLine(source, absolutePosition, width, paragraphProps, previousLineBreak: null);
        int consumed = textLine.Length;
        if (consumed <= 0) { textLine.Dispose(); break; }

        var bounds = new Rect(x, currentY, Math.Max(0, textLine.WidthIncludingTrailingWhitespace), textLine.Height);
        lines.Add(new TextLineLayout(TextSpan.FromBounds(absolutePosition, absolutePosition + consumed), bounds, lineIndex, textLine, absolutePosition, consumed));

        absolutePosition += consumed;
        currentY += textLine.Height;
        lineIndex++;
    }

    return new LayoutParagraphBlock(block.Span, new Rect(x, y, width, Math.Max(0, currentY - y)), lines);
}
```

### 11.2 CodeFenceLayouter

负责：

- 外层卡片结构尺寸
- toolbar rect
- gutter 宽度
- 每个逻辑行的 `TextLine`
- LayoutCodeFenceBlock 生成

模板中已包含完整代码。

### 11.3 第一版取舍

- paragraph 正常折行
- code fence 第一版可保持不折行策略
- absolute offset 映射第一版简化处理，后续可改成基于 `SourceText.Lines` 精确映射

---

## 12. Rendering

### 12.1 ParagraphRenderer

```csharp
public sealed class ParagraphRenderer
{
    public void RenderParagraph(DrawingContext dc, LayoutParagraphBlock block)
    {
        foreach (var line in block.Lines)
            line.TextLine.Draw(dc, new Point(line.Bounds.X, line.Bounds.Y), InvertAxes.None);
    }
}
```

### 12.2 HeadingRenderer

- 绘制 heading 文本
- level 1/2 可额外画分隔线

### 12.3 CodeFenceRenderer

负责：

- 卡片背景
- toolbar 背景
- toolbar label
- 行号 gutter
- 代码行绘制

模板中已包含完整代码。

---

## 13. Selection / Caret

### 13.1 SelectionGeometryProvider

职责：

- 遍历所有可见 block 和 line
- 找 selection span 与 line span 的交集
- 生成 rect 列表

### 13.2 CaretGeometryProvider

职责：

- 根据 offset 找到 line
- 通过近似字符宽度计算 caret x
- 返回 caret rect

### 13.3 第一版说明

当前方案是“近似字符宽度版”，适合第一阶段快速闭环。后续可升级为更精确的 `TextLine` 指标命中测试。

---

## 14. Hit Test

### 14.1 服务接口

```csharp
public interface IEditorHitTestService : IHitTestAdapter
{
}
```

### 14.2 策略

- 先命中 block
- 再命中 line
- 最后通过 line bounds 与长度估算字符偏移

模板中的 `EditorHitTestService.cs` 已实现该策略。

---

## 15. Wpf Surface

### 15.1 EditorSurfaceControl

核心职责：

- 继承 `FrameworkElement`
- 重写 `OnRender`
- 绘制正文 / selection / caret
- 捕获输入事件并 dispatch action
- 管理 caret blink

### 15.2 渲染顺序

1. Paragraph / Heading / CodeFence
2. Selection
3. Caret

### 15.3 输入事件

- `OnMouseDown`：点击定位
- `OnMouseMove`：拖选
- `OnMouseUp`：结束拖选
- `OnTextInput`：发 `ReplaceSelectionWithTextAction`
- `OnKeyDown`：方向、删除、回车、撤销重做

### 15.4 CaretBlinkController

基于 `DispatcherTimer` 实现 530ms 闪烁。

---

## 16. 测试策略

### 16.1 Text 层

- Replace
- 多 edit 应用
- dirty span
- version 前进

### 16.2 Syntax 层

- heading
- paragraph
- code fence
- reparse window 扩张

### 16.3 Editing 层

- ReplaceSelectionWithText
- DeleteBackward / DeleteForward
- InsertLineBreak
- MoveCaret
- Undo / Redo

### 16.4 Layout 层

- paragraph 折行
- code fence 行数
- selection rect
- caret rect

### 16.5 WPF 层

只做少量 GUI 测试：

- 点击定位
- 拖选
- 输入
- Ctrl+Z / Ctrl+Y

---

## 17. 首批必须落地的文件

### 第一批

- Core.Text
  - DocumentSnapshot.cs
  - SelectionRange.cs
  - TextEdit.cs
  - ITextBufferService.cs
  - SourceTextBufferService.cs
- Core.Syntax
  - ParseResult.cs
  - MdNode.cs
  - ParagraphBlock.cs
  - HeadingBlock.cs
  - CodeFenceBlock.cs
  - MarkdownParser.cs
  - ReparseWindowExpander.cs
- Core.Editing
  - EditorState.cs
  - Actions.cs
  - UndoRedoService.cs
  - EditorReducer.cs
- Core.Layout
  - Model.cs
  - TextRuns.cs
  - TextSources.cs
  - LayoutEngine.cs
- Wpf
  - MainWindow.xaml
  - EditorSurfaceControl.cs
  - EditorHitTestService.cs
  - ParagraphRenderer.cs
  - CodeFenceRenderer.cs
  - SelectionRenderer.cs
  - CaretRenderer.cs

---

## 18. 开工顺序

最优先顺序：

1. Core.Text
2. Core.Syntax
3. Core.Editing
4. Core.Layout
5. Wpf Surface

推荐具体顺序：

1. DocumentSnapshot
2. SourceTextBufferService
3. ParagraphBlock / HeadingBlock / CodeFenceBlock
4. MarkdownParser
5. EditorState
6. EditorReducer
7. UndoRedoService
8. ParagraphTextSource
9. ParagraphLayouter
10. CodeLineTextSource
11. CodeFenceLayouter
12. LayoutEngine
13. EditorSurfaceControl
14. Renderers
15. HitTest / Selection / Caret

---

## 19. 第一阶段妥协

- 命中测试近似字符宽度
- undo/redo 全量 dirty
- inline parser 先整块重算
- Delete 按 UTF-16 单位
- code fence 先不做复杂块内编辑增强

这些妥协不影响架构方向。

---

## 20. 技术结论

- `SourceText` 做文本快照层：正确
- Markdown block 增量重算：正确
- Action/Reducer 数据层：正确
- WPF `TextFormatter` 自定义布局：正确
- `FrameworkElement.OnRender` 自绘 surface：正确
- `ImmutableStack<T>` 做历史：正确

> 当前方案已经足够支撑一个“可输入、可删、可回车、可撤销、可显示 Markdown block”的原生 WPF 原型。
